const accountsBaseUrl = '/api/accounts';
const sessionsBaseUrl = '/api/sessions';
const devicesBaseUrl = '/api/devices';

module.exports = {
    accountsBaseUrl,
    devicesBaseUrl,
    sessionsBaseUrl,
}